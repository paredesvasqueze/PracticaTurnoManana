@model IEnumerable<Models.Medico>

@{
    ViewData["Title"] = "Listado de Médicos";
}

<div class="container mt-4">
    <h2 class="mb-4 text-center">@ViewData["Title"]</h2>

    <div class="text-end mb-3">
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Nuevo Médico
        </a>
    </div>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Especialidad</th>
                <th>Teléfono</th>
                <th>Correo</th>
                <th>Horario</th>
                <th style="width:180px;">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var medico in Model)
                {
                    <tr>
                        <td>@medico.nIdMedico</td>
                        <td>@medico.cNombre</td>
                        <td>@medico.cApellido</td>
                        <td>@medico.cEspecialidad</td>
                        <td>@medico.cTelefono</td>
                        <td>@medico.cCorreo</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@medico.nIdMedico" class="btn btn-sm btn-warning">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                            <a asp-action="Details" asp-route-id="@medico.nIdMedico" class="btn btn-sm btn-info">
                                <i class="bi bi-eye"></i> Ver
                            </a>
                            <a asp-action="Delete" asp-route-id="@medico.nIdMedico" class="btn btn-sm btn-danger">
                                <i class="bi bi-trash"></i> Eliminar
                            </a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="7" class="text-center text-muted">No hay médicos registrados.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
